<resources xmlns:tools="http://schemas.android.com/tools">
    
    <style name="Theme.FoodDelivery" parent="Theme.FoodDelivery.Base">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="background">@color/background</item>
        <item name="colorOnBackground">@color/onBackground</item>
        <item name="colorSurface">@color/background</item>
        <item name="colorOnSurface">@color/onSurface</item>
        <item name="colorOnSurfaceVariant">@color/onSurfaceVariant</item>

        <item name="android:statusBarColor">?attr/background</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="bottomNavigationStyle">@style/BottomNavigation</item>
    </style>
    
    <!-- Base application theme. -->
    <style name="Theme.FoodDelivery.Base" parent="Theme.MaterialComponents.Light.NoActionBar">
    </style>
    
</resources>